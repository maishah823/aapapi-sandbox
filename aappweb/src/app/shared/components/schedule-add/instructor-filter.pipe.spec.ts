import { InstructorFilterPipe } from './instructor-filter.pipe';

describe('InstructorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InstructorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
