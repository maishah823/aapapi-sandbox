import { EventLineSortPipe } from './event-line-sort.pipe';

describe('EventLineSortPipe', () => {
  it('create an instance', () => {
    const pipe = new EventLineSortPipe();
    expect(pipe).toBeTruthy();
  });
});
